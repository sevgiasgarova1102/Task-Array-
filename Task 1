#include <iostream>
using namespace std;

void selectionSort(int a[], int n) {
    for(int i = 0; i < n - 1; i++){
        int maxİndex = i;

        for(int j = i + 1; j < n; j++){
            if(a[j] > a[maxIndex]) {
                maxİndex = j;
            }
        }

        int t = a[i];
        a[i] = a[maxİndex];
        a[maxİndex] = t;
    }
}

int main() {
    int a[8] = {11,0,-3,2,4,-6,3,-2};

    selectionSort(a, 8);

    for(int i = 0; i < 8; i++){
        cout << a[i] << " ";
    }
}
